# Cash Flow

OCF = Collections - Operating outflows
FCF = OCF + Development CF
Peak Funding = min(cumulative levered cash) absolute value, period of min.
DSCR = OCF / Debt Service (if DS=0 → inf if OCF>0 else None) via cashflow_analysis.py.
Monthly cash via integrated_model periods (MonthlyRow).
