# Scenario Analysis

Base/Best/Worst/Stress via ModelRunner.run_scenarios — each scenario rebuilds IntegratedRealEstateModel with new assumptions (no KPI scaling: no profit*=factor).
Test: selling_price*1.10 → GDV*1.10 via rebuild.
