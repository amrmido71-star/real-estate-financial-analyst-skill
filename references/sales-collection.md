# Sales & Collection

Contracted sales = sum(unit price_per_unit where sold). Monthly sales = sum per sale_dates (sales_by_label[YYYY-MM]) — not avg_price*units.
Collection schedule via build_collection_schedule (down % + installments + handover), sums to 100%.
Aging buckets 0-30/31-60 etc., Velocity trend via sales_collection.
